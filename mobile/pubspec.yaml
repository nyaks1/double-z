name: double_z
description: "A premium zero-trust, stateless payment voice agent."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Design
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6
  
  # Core Logic
  http: ^1.2.1
  record: ^5.0.5
  path_provider: ^2.1.3
  solana_mobile_client: ^1.0.0 # Hypothetical or exact version if available (or mwa variant)
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
